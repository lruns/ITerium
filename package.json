{
  "name": "lruns-iterium-vitrina",
  "version": "0.5.3",
  "private": true,
  "description": "Lruns ITerium - museum of code, beauty and cringe (beta)",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --format=iife --target=es2019 --loader:.jpg=dataurl --outfile=bundle.js",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run build"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
